arrays in js 
array objects reference,
foreach loops in js,

array methods/properties 
typeof(arr),what is key of arr if array is objects,
length,sort,push,pop,reverse(),unshift(add),shift, 
delete arr[2], slice(start,end),
splice(idx,n-del,x1,x2,x2..)
indexof,lastindexof(key);
